extends pug/layout.pug

block variables
  -
    const page = 'Карточка товара'
    const pageTitle = 'Maroon: информация о товаре'

block content
  +header(page)
  main
    +pageTitle(pageTitle)
    +cardDetails
    +offers
  +footer
